---
const { title } = Astro.props;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <title>{title}</title>
    <style>
      :root {
        font-family: system-ui, -apple-system, Segoe UI, Roboto, sans-serif;
        max-width: 900px;
        margin: 0 auto;
        padding: 2rem;
        line-height: 1.6;
      }
      a { text-decoration: none; }
      h1 { margin-bottom: 0.2rem; }
      .meta { opacity: 0.6; font-size: 0.9rem; }
      .card {
        border: 1px solid #e5e5e5;
        padding: 1rem;
        border-radius: 10px;
        margin-bottom: 1rem;
      }
      footer {
        margin-top: 4rem;
        opacity: 0.6;
        font-size: 0.9rem;
      }
    </style>
  </head>
  <body>
    <slot />
    <footer>
      © {new Date().getFullYear()} Jason P Dicker — Built with Astro.
    </footer>
  </body>
</html>

